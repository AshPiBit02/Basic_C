#include<stdio.h>
void main()
{
    int a=10,c,b=20,*x,*y;
    printf("Before swapping a=%d & b=%d\n",a,b);
    // c=a;
    // a=b;
    // b=c;


    // a=a+b;
    // b=a-b;
    // a=a-b;
    printf("After swappine a=%d & b=%d\n",a,b);
}