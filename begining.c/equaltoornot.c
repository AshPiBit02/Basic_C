//To find a number is equals to 11 or not.
#include<stdio.h>
void main()
{
    int a;
    printf("Enter any number");
    scanf("%d",&a);
    if(a==11)
    {
        printf("%d is equals to eleven",a);
    }
    else
    {
        printf("%d doesn't equals to eleven",a);
    }
}